    <div class="text-white">
        <h1>{{{form_h1}}}</h1>
        <p>{{{form_p}}}</p>
    </div>

    {{#if error_message}}
            {{#ifNotEquals error_message ""}}
            <div class="alert alert-danger" role="alert">
                {{error_message}}
            </div>
            {{/ifNotEquals}}
    {{/if}}


    <div class="container">
        <div class="row">
            <div class="col-md-4">
                <h2>Your password has been reset!</h2>
            </div>
            <div class="col-md-4">
                <h2>What next?</h2>
                <p>Access your <a href='/account'>account</a>.</p>
            </div>
        </div>
    </div>